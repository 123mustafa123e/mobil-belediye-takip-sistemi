import 'package:flutter/material.dart';

class AppColors {
  // Temel Renkler
  static const Color primary = Color(0xFF1565C0);
  static const Color secondary = Color(0xFF0288D1);
  static const Color accent = Color(0xFFFF6F00);
  static const Color background = Color(0xFFF5F7FA);

  // Durum Renkleri
  static const Color statusPending = Color(0xFFFFA726); // Bekliyor
  static const Color statusInProgress = Color(0xFF29B6F6); // Devam Ediyor
  static const Color statusResolved = Color(0xFF66BB6A); // Çözüldü
  static const Color statusRejected = Color(0xFFEF5350); // Reddedildi
  static const Color statusOnHold = Color(0xFFAB47BC); // Beklemede
}
